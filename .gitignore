# Runtime state, not tooling: boot-vm.sh and boot-topo.sh put console and
# monitor sockets, pidfiles and console logs under run/.
/run/

# Python bytecode from running the checks in place
__pycache__/
*.pyc
